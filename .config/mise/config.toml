# Minimum mise version required for this config
min_version = "2026.1.0"

[tools]
python = { version = "3.13" }

"aqua:dalance/procs" = "latest"  # Modern ps replacement
"cargo:cbfmt" = "latest"
lychee = "latest"
"github:EmbarkStudios/cargo-deny" = "latest"
"github:Kampfkarren/selene" = "latest"
"github:agavra/tuicr" = "latest"
betterleaks = "latest"
"github:dlvhdr/diffnav" = "latest"
"github:dolthub/dolt" = "latest"
"github:editorconfig-checker/editorconfig-checker" = "latest"
"github:gastownhall/beads" = "latest"
"github:jackchuka/mdschema" = "latest"
"github:nolabs-ai/nono" = "v0.65.1"
"github:owenlamont/ryl" = "latest"
"github:stacklok/modelith" = "latest"
"go:github.com/grafana/mcp-grafana/cmd/mcp-grafana" = "v0.16.0"
"npm:@arizeai/phoenix-cli" = "latest"
"npm:@contextlint/cli" = "latest"
"npm:@devcontainers/cli" = "latest"
"npm:@doist/todoist-cli" = "latest"
codex = "latest"
"npm:ccusage" = "latest"
"npm:confluence-cli" = "latest"
"npm:dclint" = "latest"
"npm:hunkdiff" = "latest"
"npm:playwright" = "latest"
"npm:promptfoo" = "latest"
"npm:reveal.js" = "latest"
"npm:rulesync" = "latest"
"pipx:agentsview" = "latest"
"pipx:aws-sso-util" = "latest"
"pipx:basedpyright" = "latest"  # Enhanced fork of pyright with additional features
"pipx:basic-memory" = "latest"
"pipx:crawl4ai" = "latest"
"pipx:deply" = "latest"
"pipx:desloppify" = "latest"
"pipx:hindsight-embed" = { version = "latest", uvx_args = "--with hindsight-api --with langchain-text-splitters" }
"pipx:prowler" = "latest"
"pipx:pyrefly" = "latest"
ruff = "latest"
"pipx:sparkrun" = "latest"
"pipx:sshuttle" = "latest"
"pipx:ty" = "latest"
yamllint = "latest"  # YAML linter
# Co-install hindsight-api + langchain-text-splitters into the embed venv so the
# `uvx hindsight-api@VER` env. This (1) survives uv cache GC that previously left
# daemon launches the local hindsight-api binary instead of an isolated runtime
# the daemon running from a deleted archive, and (2) provides langchain-text-splitters,
# which hindsight-api imports lazily but never declares as a dependency.
1password = "latest"
act = "latest"
action-validator = "latest"
actionlint = "latest"
amazon-ecr-credential-helper = "latest"
ast-grep = "latest"
atuin = "latest"
aws-cli = "2"  # Pinned to major version for stability
bat = "latest"
bat-extras = "latest"
bottom = "latest"  # Modern top/htop replacement
buf = "latest"
bun = "latest"
checkov = "latest"  # IaC security scanner
cue = "latest"
delta = "latest"
difftastic = "latest"
direnv = "latest"
docker-compose = "latest"
dotnet = "latest"
dua = "latest"
duckdb = "latest"
duf = "latest"  # Modern df replacement (Go-based)
dust = "latest"  # Modern du replacement
editorconfig-checker = "latest"
eza = "latest"  # Modern ls replacement (fork of exa)
fd = "latest"
fzf = "latest"
ghalint = "latest"
ghorg = "latest"
gitleaks = "latest"
glow = "latest"
go = "latest"
grype = "latest"
hadolint = "latest"  # Dockerfile linter
helm = "3"  # Pinned to major version for stability
herdr = "latest"
hk = "latest"
java = "17.0.2"
jq = "latest"
just = "latest"
k3d = "latest"
kubectl = "1.31"  # Pinned - should match cluster version
kubectx = "latest"
lua = "latest"
make = "latest"
maven = "latest"
neovim = "latest"
node = "latest"
oh-my-posh = "latest"
opa = "latest"
opencode = "latest"
pandoc = "latest"
patat = "latest"
pinact = "latest"
pkl = "latest"
podman = "latest"
pre-commit = "latest"
pulumi = "latest"
rclone = "latest"
ripgrep = "latest"
ripgrep-all = "latest"
rumdl = "latest"
rust = { version = "latest", targets = "wasm32-wasip1" }
semgrep = "latest"
shellcheck = "latest"
shfmt = "latest"
skaffold = "latest"
sqlite = "latest"
stylua = "latest"
swift = "latest"
syft = "latest"
television = "latest"  # Modern fuzzy finder (tv)
terraform = "latest"
tflint = "latest"  # Terraform linter
tmux = "latest"
tombi = "latest"
trivy = "latest"
trufflehog = "latest"
typos = "latest"
usage = "latest"
uv = "latest"
yamlfmt = "latest"
zizmor = "latest"
zoxide = "latest"
npm = "latest"
mkcert = "latest"
vacuum = "latest"
gofumpt = "latest"
golangci-lint = "latest"
"go:golang.org/x/tools/cmd/goimports" = "latest"
"go:golang.org/x/vuln/cmd/govulncheck" = "latest"
"go:github.com/securego/gosec/v2/cmd/gosec" = "latest"
"go:github.com/google/go-licenses" = "latest"
"aqua:anordal/shellharden" = "latest"
markdownlint-cli2 = "latest"
worktrunk = "latest"
"github:1broseidon/ketch" = "latest"
marksman = "latest"
"go:golang.org/x/tools/gopls" = "latest"
"pipx:showboat" = "latest"
"github:grafana/gcx" = "latest"
"github:boyter/scc" = "latest"
"go:github.com/jbeda/mdreflow/cmd/mdreflow" = "v0.2.0"
ruby = "3.3"
"github:kenn-io/roborev" = "latest"
glab = "latest"
goreleaser = "latest"
"go:golang.org/x/tools/cmd/deadcode" = "latest"

[env]
HK_MISE = "1"

[settings]
# plugins can read the versions files used by other version managers (if enabled by the plugin)
# for example, .nvmrc in the case of node's nvm

# configure `mise install` to always keep the downloaded archive
always_keep_download = false  # deleted after install by default
always_keep_install = false  # deleted on failure by default

# configure how frequently (in minutes) to fetch updated plugin repository changes
# this is updated whenever a new runtime is installed
plugin_autoupdate_last_check_duration = "1 week"  # set to 0 to disable updates

verbose = false  # set to true to see full installation output, see `MISE_VERBOSE`
http_timeout = "30s"  # set the timeout for http requests as duration string, see `MISE_HTTP_TIMEOUT`
jobs = 2  # number of plugins or runtimes to install in parallel
raw = false  # set to true to directly pipe plugins to stdin/stdout/stderr
yes = false  # set to true to automatically answer yes to all prompts

not_found_auto_install = true  # automatically install missing tools when command not found
paranoid = false  # see MISE_PARANOID

# disable_default_shorthands = false # disable the default shorthands, see `MISE_DISABLE_DEFAULT_SHORTHANDS`

env_file = ".env"  # load env vars from a dotenv file, see `MISE_ENV_FILE`

experimental = true  # enable experimental features

# configure messages displayed when entering directories with config files
status = { missing_tools = "if_other_versions_installed", show_env = false, show_tools = false }

# Enable reading native version files for these tools (.nvmrc, .python-version, etc.)
idiomatic_version_file_enable_tools = ["python", "node", "go", "java"]
lockfile = true
locked_verify_provenance = true

# Supply-chain cooldown: ignore versions newer than this window so the
# community has time to flag compromised releases before mise installs them.
# Applies to version-resolved backends (npm, pipx, cargo crates.io, github
# releases, etc). Does NOT affect `branch:main` git installs (no release date).
# NOTE: the per-tool exemption list (minimum_release_age_excludes) may or may
# not be supported by the current mise version -- check `mise doctor` output.
# If a security scanner's freshness matters more than the cooldown, pin it to
# an explicit recent version.
minimum_release_age = "7d"

[settings.github]
use_git_credentials = true

[tasks.upgrade]
description = "Upgrade all tools to latest versions"
run = "mise upgrade --bump"

[tasks.doctor]
description = "Check mise health and configuration"
run = "mise doctor"

[tasks.outdated]
description = "Show outdated tools"
run = "mise outdated"

[tasks.prune]
description = "Remove unused tool versions"
run = "mise prune"

# ── dotfiles repo ────────────────────────────────────────────────────────────

[bootstrap.repos."~/.dotfiles"]
url = "git@github-personal:mattniedelman/dotfiles.git"

# ── apt repo setup (pre-packages phase) ─────────────────────────────────────

[bootstrap.directories."/etc/apt/keyrings"]
owner = "root"
group = "root"
mode = "0755"
phase = "pre-packages"

[bootstrap.hooks.pre-packages]
run = [
  "sudo curl -fsSL 'https://keyserver.ubuntu.com/pks/lookup?fingerprint=on&op=get&search=0x88421E703EDC7AF54967DED473C9FCC9E2BB48DA' -o /etc/apt/keyrings/fish-shell.asc.tmp && sudo chmod 0644 /etc/apt/keyrings/fish-shell.asc.tmp && sudo mv /etc/apt/keyrings/fish-shell.asc.tmp /etc/apt/keyrings/fish-shell.asc",
  "sudo curl -fsSL 'https://cli.github.com/packages/githubcli-archive-keyring.gpg' -o /etc/apt/keyrings/githubcli-archive-keyring.gpg.tmp && sudo chmod 0644 /etc/apt/keyrings/githubcli-archive-keyring.gpg.tmp && sudo mv /etc/apt/keyrings/githubcli-archive-keyring.gpg.tmp /etc/apt/keyrings/githubcli-archive-keyring.gpg",
  "sudo curl -fsSL 'https://pkgs.tailscale.com/stable/ubuntu/noble.noarmor.gpg' -o /etc/apt/keyrings/tailscale-archive-keyring.gpg.tmp && sudo chmod 0644 /etc/apt/keyrings/tailscale-archive-keyring.gpg.tmp && sudo mv /etc/apt/keyrings/tailscale-archive-keyring.gpg.tmp /etc/apt/keyrings/tailscale-archive-keyring.gpg",
  "sudo curl -fsSL 'https://downloads.1password.com/linux/keys/1password.asc' -o /etc/apt/keyrings/1password.asc.tmp && sudo chmod 0644 /etc/apt/keyrings/1password.asc.tmp && sudo mv /etc/apt/keyrings/1password.asc.tmp /etc/apt/keyrings/1password.asc",
  "sudo curl -fsSL 'https://keyserver.ubuntu.com/pks/lookup?fingerprint=on&op=get&search=0x0721FDF5FECB88DC6920361657C8EF455CEAE491' -o /etc/apt/keyrings/ghostty.asc.tmp && sudo chmod 0644 /etc/apt/keyrings/ghostty.asc.tmp && sudo mv /etc/apt/keyrings/ghostty.asc.tmp /etc/apt/keyrings/ghostty.asc",
  "sudo curl -fsSL 'https://raw.githubusercontent.com/imputnet/helium-linux/main/pubkey.asc' -o /etc/apt/keyrings/helium.asc.tmp && sudo chmod 0644 /etc/apt/keyrings/helium.asc.tmp && sudo mv /etc/apt/keyrings/helium.asc.tmp /etc/apt/keyrings/helium.asc",
]

# fish shell 4.x PPA — Ubuntu noble (24.04) only
# If upgrading Ubuntu: update Suites and check for a new PPA release series
[bootstrap.files."/etc/apt/sources.list.d/fish-shell.sources"]
source = "./files/apt/fish-shell.sources"
owner = "root"
group = "root"
mode = "0644"
phase = "pre-packages"

# GitHub CLI apt repo; keyring is refreshed by the pre-packages hook
[bootstrap.files."/etc/apt/sources.list.d/github-cli.sources"]
source = "./files/apt/github-cli.sources"
owner = "root"
group = "root"
mode = "0644"
phase = "pre-packages"

# Tailscale VPN
[bootstrap.files."/etc/apt/sources.list.d/tailscale.sources"]
source = "./files/apt/tailscale.sources"
owner = "root"
group = "root"
mode = "0644"
phase = "pre-packages"

# 1Password
[bootstrap.files."/etc/apt/sources.list.d/1password.sources"]
source = "./files/apt/1password.sources"
owner = "root"
group = "root"
mode = "0644"
phase = "pre-packages"

# Ghostty terminal — community PPA (noble/24.04 only)
[bootstrap.files."/etc/apt/sources.list.d/ghostty.sources"]
source = "./files/apt/ghostty.sources"
owner = "root"
group = "root"
mode = "0644"
phase = "pre-packages"

# Helium browser
[bootstrap.files."/etc/apt/sources.list.d/helium.sources"]
source = "./files/apt/helium.sources"
owner = "root"
group = "root"
mode = "0644"
phase = "pre-packages"

# ── system packages ───────────────────────────────────────────────────────────

[bootstrap.packages]
# Shell and bootstrap prerequisites
"apt:fish" = "latest"
"apt:curl" = "latest"
"apt:git" = "latest"
"apt:ca-certificates" = "latest"
"apt:unzip" = "latest"
# Build deps required by cargo/go/node native builds
"apt:build-essential" = "latest"
"apt:libssl-dev" = "latest"
"apt:pkg-config" = "latest"
# System utilities
"apt:wl-clipboard" = "latest"
"apt:inotify-tools" = "latest"
"apt:gh" = "latest"
"apt:git-lfs" = "latest"
"apt:wget" = "latest"
"apt:gnupg" = "latest"
"apt:zip" = "latest"
"apt:xz-utils" = "latest"
"apt:lsb-release" = "latest"
"apt:pv" = "latest"
"apt:tree" = "latest"
"apt:parallel" = "latest"
# Build deps for Python/Ruby/native extensions
"apt:zlib1g-dev" = "latest"
"apt:libffi-dev" = "latest"
"apt:libreadline-dev" = "latest"
"apt:libsqlite3-dev" = "latest"
"apt:python3-dev" = "latest"
# Dev tools
"apt:clang" = "latest"
"apt:protobuf-compiler" = "latest"
"apt:postgresql-client" = "latest"
# Container runtime
"apt:podman" = "latest"
"apt:tailscale" = "latest"
# 1Password GUI; CLI is installed by the `1password` mise tool above
"apt:1password" = "latest"
# Terminal emulator
"apt:ghostty" = "latest"
# Browsers
"apt:helium-bin" = "latest"
# C/C++ build stack (neovim plugins, tree-sitter)
"apt:cmake" = "latest"
"apt:ninja-build" = "latest"
"apt:meson" = "latest"
# Media processing
"apt:ffmpeg" = "latest"
"apt:imagemagick" = "latest"
# Networking
"apt:nmap" = "latest"
"apt:socat" = "latest"
# Database
"apt:sqlite3" = "latest"
# Diagrams
"apt:graphviz" = "latest"
# Terminal / TUI tools
"apt:btop" = "latest"
"apt:asciinema" = "latest"
"apt:chafa" = "latest"
"apt:hexyl" = "latest"
# Neovim
"apt:luarocks" = "latest"

# ── dotfiles ────────────────────────────────────────────────────────────────


[dotfiles]
# Plain symlinks — source inferred from dotfiles.root (mirrors ~/.*)
"~/.bashrc" = { mode = "symlink" }

# ~/.claude — symlink-each; exclude all runtime/generated state
"~/.claude" = { mode = "symlink-each", exclude = [
  "hooks/*.tera",
  "sessions/**", "projects/**", "plugins/cache/**", "plugins/data/**", "plugins/marketplaces/**",
  "backups/**", "cache/**", "context-mode/**", "debug/**", "downloads/**", "file-history/**",
  "openspec/**", "paste-cache/**", "plans/**", "powerline/**", "shell-snapshots/**",
  "session-env/**", "tasks/**", "telemetry/**", "usage-data/**",
  ".beads/**", ".ralph/**", ".serena/**", ".claude/**",
  ".git/**", ".gitignore", "headroom_memory*", "history.jsonl",
  "stats-cache.json", "hook-approvals.log", "security_warnings_state_*",
  "__pycache__/**", "*.pyc", "install-counts-cache.json",
] }
"~/.config/ast-grep/rules" = { mode = "symlink" }
"~/.config/ast-grep/sgconfig.yml" = { mode = "symlink" }
"~/.config/atuin" = { mode = "copy" }
"~/.config/atuin/config.toml" = { mode = "track" }

# ~/.config — file/dir-level entries
"~/.config/cbfmt.toml" = { mode = "symlink" }

# ~/.config/fish — symlink-each so fish can write fish_variables, completions/, etc.
"~/.config/fish" = { mode = "symlink-each", exclude = ["fish_variables", "fish_variables.*", "completions/**", "logs/**", "themes/**"] }

# ~/.config — copy mode (edit in-place; tracked for local undo history)
"~/.config/ghostty" = { mode = "copy" }
"~/.config/ghostty/config" = { mode = "track" }
"~/.config/git" = { mode = "symlink" }
"~/.config/herdr/config.toml" = { mode = "copy" }
"~/.config/kitty" = { mode = "symlink" }
"~/.config/kubernetes-mcp-server" = { mode = "symlink" }
"~/.config/mdsf" = { mode = "symlink" }
"~/.config/mise/.default-python-packages" = { mode = "symlink" }
"~/.config/mise/config.toml" = { mode = "symlink" }
"~/.config/mise/files" = { mode = "symlink" }
"~/.config/mise/conf.d/dotfiles-tracking.toml" = { mode = "symlink" }

# ~/.config — whole-directory symlinks (set-and-forget, edit via source)
"~/.config/nvim" = { mode = "symlink-each", manifest = "git" }
"~/.config/ohmyposh" = { mode = "copy" }
"~/.config/ohmyposh/claude.json" = { mode = "track" }
"~/.config/television" = { mode = "symlink" }

# Home root
"~/.gitconfig" = { mode = "symlink" }

# ~/.omp — symlink-each; agent/ has private files (config.yml, mcp.json)
"~/.omp" = { mode = "symlink-each" }
"~/.profile" = { mode = "symlink" }
"~/.ripgreprc" = { mode = "symlink" }
# ~/.config/systemd/user — template services that can't be expressed in
# [bootstrap.linux.systemd.units] (instantiated template, no WantedBy)
"~/.config/systemd/user/notify-rclone-failure@.service" = { mode = "symlink" }

# ── systemd user services (replaces chezmoi-placed unit files) ──────────────

# [bootstrap.linux.systemd.units.basic-memory]
# description = "Basic Memory MCP Server (Streamable HTTP)"
# exec_start = "~/.local/share/mise/shims/basic-memory mcp --transport streamable-http --host 127.0.0.1"
# after = ["network.target"]
# restart = "on-failure"
# restart_sec = "5s"


# ── directories for services that need pre-existing paths ────────────────────

[bootstrap.directories."~/onedrive"]
mode = "0755"

[bootstrap.directories."~/.cache/rclone/imprivata-onedrive"]
mode = "0755"

[bootstrap.directories."~/.beads/shared-server"]
mode = "0755"

# ── additional systemd user services ─────────────────────────────────────────

# [bootstrap.linux.systemd.units.agentsview]
# description = "AgentsView - local web viewer for AI agent sessions"
# after = ["network-online.target"]
# wants = ["network-online.target"]
# exec_start = "%h/.local/share/mise/shims/agentsview serve --no-browser --host 127.0.0.1 --port 8080"
# restart = "on-failure"
# restart_sec = "5s"
# timeout_start_sec = "120"

# [bootstrap.linux.systemd.units.hindsight-embed-daemon]
# description = "Hindsight embed daemon (claude-code profile)"
# after = ["network.target"]
# type = "oneshot"
# remain_after_exit = true
# exec_start = "~/.local/bin/hindsight-start claude-code"
# exec_stop = "~/.local/share/mise/shims/hindsight-embed -p claude-code daemon stop"
#
# [bootstrap.linux.systemd.units.hindsight-embed-ui]
# description = "Hindsight embed UI (claude-code profile)"
# after = ["network.target", "dev.mise.hindsight-embed-daemon.service"]
# requires = ["dev.mise.hindsight-embed-daemon.service"]
# type = "oneshot"
# remain_after_exit = true
# exec_start = "~/.local/share/mise/shims/hindsight-embed -p claude-code ui start"
# exec_stop = "~/.local/share/mise/shims/hindsight-embed -p claude-code ui stop"

# [bootstrap.linux.systemd.units.kenn-forge]
# description = "kenn-forge daemon"
# after = ["network-online.target"]
# wants = ["network-online.target"]
# exec_start = "~/.local/bin/kenn-forge serve"
# restart = "on-failure"
# restart_sec = "5s"

# [bootstrap.linux.systemd.units.sparkrun-proxy]
# description = "sparkrun LiteLLM inference proxy"
# after = ["network-online.target"]
# wants = ["network-online.target"]
# exec_start = "~/.local/share/mise/shims/sparkrun proxy start --foreground --host 127.0.0.1"
# restart = "on-failure"
# restart_sec = "5s"
# environment = { HOME = "%h", PATH = "%h/.local/share/mise/shims:%h/.local/bin:/usr/local/bin:/usr/bin:/bin" }
#
#
# [bootstrap.linux.systemd.units.rclone-imprivata-onedrive]
# description = "Rclone mount for Imprivata OneDrive"
# after = ["network.target"]
# type = "notify"
# exec_start = "~/.local/share/mise/shims/rclone mount imprivata-onedrive: %h/onedrive --config %h/.config/rclone/rclone.conf --cache-dir %h/.cache/rclone/imprivata-onedrive --vfs-cache-mode full --vfs-refresh"
# exec_stop = "/usr/bin/fusermount3 -u %h/onedrive"
# restart = "on-failure"
# restart_sec = "5s"
#
# [bootstrap.linux.systemd.units.keylight-webcam]
# description = "Elgato Key Light follows Insta360 webcam use"
# after = ["network-online.target"]
# wants = ["network-online.target"]
# exec_start = "%h/.local/bin/keylight-webcam.sh"
# exec_stop = "%h/.local/bin/keylight-webcam.sh off"
# restart = "on-failure"
# restart_sec = "5s"

# aws-sso-keepwarm: oneshot service driven by a timer
# Note: OnStartupSec (fires on user manager start) is unsupported; using
# on_unit_inactive_sec only — first run fires ~15min after login.
# [bootstrap.linux.systemd.units.aws-sso-keepwarm]
# description = "Keep AWS SSO imprivata access token warm for headless/sandboxed use"
# after = ["network-online.target"]
# wants = ["network-online.target"]
# type = "oneshot"
# exec_start = "%h/.local/bin/aws-sso-keepwarm.sh"
# environment = { XDG_RUNTIME_DIR = "/run/user/1001" }
# nice = 10
# wanted_by = []
#
# [bootstrap.linux.systemd.units.aws-sso-keepwarm-timer]
# description = "Periodically refresh the AWS SSO imprivata access token"
# on_unit_inactive_sec = "15min"
# persistent = true
# unit = "aws-sso-keepwarm"
#
# [bootstrap.linux.systemd.units.sshuttle-dev]
# description = "sshuttle tunnel to ai-nonprod-vpc"
# after = ["network-online.target"]
# wants = ["network-online.target"]
# exec_start = "%h/.local/bin/sshuttle-dev"
# restart = "on-failure"
# restart_sec = "15s"
# environment = { PATH = "%h/.local/share/mise/shims:%h/.local/bin:/usr/local/bin:/usr/bin:/bin" }

# ── history watcher service ──────────────────────────────────────────────────

[bootstrap.services.mise-history]
builtin = "history-watch"

# ── reload hooks ─────────────────────────────────────────────────────────────
# Run after `mise dot apply` or `mise dot rollback` modifies matching files.

[history.reload]
# Herdr multiplexer server picks up config without restart
"~/.config/herdr/**" = "herdr server reload-config"

# ── user settings ────────────────────────────────────────────────────────────

[bootstrap.user]
login_shell = "/usr/bin/fish"

# ── SSH keys task (replaces authorized_keys template) ───────────────────────

[tasks.sync-ssh-keys]
description = "Fetch GitHub SSH keys into authorized_keys (run on servers you SSH into)"
run = """
mkdir -p ~/.ssh && chmod 700 ~/.ssh
gh api /users/mattniedelman/keys --jq '.[].key' > ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
"""

# ── bootstrap verification task ──────────────────────────────────────────────

[tasks.bootstrap]
description = "Post-bootstrap verification"
run = "mise doctor"
