[tools]
python = { version = '3.13', install_env = { MISE_PYTHON_COMPILE = "0", MISE_PYTHON_PRECOMPILED_FLAVOR = "freethreaded+pgo-full" } }

# Modern CLI tool replacements (Rust/cargo packages)
"cargo:bottom" = "latest"  # Modern top/htop replacement
"cargo:du-dust" = "latest" # Modern du replacement
duf = "latest"             # Modern df replacement (Go-based)
"cargo:eza" = "latest"     # Modern ls replacement (fork of exa)
"cargo:procs" = "latest"   # Modern ps replacement
"cargo:xsv" = "latest"

1password = "latest"
act = "latest"
action-validator = "latest"
actionlint = "latest"
amazon-ecr-credential-helper = "latest"
atuin = "latest"
aws-cli = "latest"
bat = "latest"
bat-extras = "latest"
chezmoi = "latest"
cue = "latest"
dagger = "latest"
difftastic = "latest"
direnv = "latest"
docker-compose = "latest"
dua = "latest"
duckdb = "latest"
fd = "latest"
fzf = "latest"
github-cli = "latest"
glow = "latest"
go = 'latest'
helm = "latest"
java = "17.0.2"
jq = "latest"
k3d = "latest"
kube-capacity = "latest"
kube-linter = "latest"
kube-score = "latest"
kubectl = "latest"
kubesec = "latest"
loki-logcli = "latest"
lua = "latest"
make = "latest"
neovim = "latest"
node = "latest"
pandoc = "latest"
patat = "latest"
poetry = "latest"
pre-commit = "latest"
ripgrep = "latest"
ripgrep-all = "latest"
rust = "latest"
rye = "latest"
shellcheck = "latest"
shfmt = "latest"
skaffold = "latest"
sqlite = "latest"
sshuttle = "latest"
trivy = "latest"
usage = "latest"
uv = "latest"
zoxide = "latest"

# MCP Servers for Augment integration
"npm:@augmentcode/auggie" = "latest"
"npm:@modelcontextprotocol/server-filesystem" = "latest"
"npm:@modelcontextprotocol/server-sequential-thinking" = "latest"
grype = "latest"
trufflehog = "latest"
"cargo:wrkflw" = "latest"

# Python tools via pipx
"pipx:zuban" = "latest"
"pipx:basedpyright" = "latest"
"pipx:keyring" = "latest"
"pipx:aws-sso-util" = "latest"
"pipx:ruff" = "latest"



[settings]
# plugins can read the versions files used by other version managers (if enabled by the plugin)
# for example, .nvmrc in the case of node's nvm

# configure `mise install` to always keep the downloaded archive
always_keep_download = false # deleted after install by default
always_keep_install = false  # deleted on failure by default

# configure how frequently (in minutes) to fetch updated plugin repository changes
# this is updated whenever a new runtime is installed
# (note: this isn't currently implemented but there are plans to add it: https://github.com/jdx/mise/issues/128)
plugin_autoupdate_last_check_duration = '1 week' # set to 0 to disable updates

# config files with these prefixes will be trusted by default
trusted_config_paths = ['~/work/my-trusted-projects']

verbose = false      # set to true to see full installation output, see `MISE_VERBOSE`
http_timeout = "30s" # set the timeout for http requests as duration string, see `MISE_HTTP_TIMEOUT`
jobs = 4             # number of plugins or runtimes to install in parallel. The default is `4`.
raw = false          # set to true to directly pipe plugins to stdin/stdout/stderr
yes = false          # set to true to automatically answer yes to all prompts

not_found_auto_install = false # see MISE_NOT_FOUND_AUTO_INSTALL
task_output = "prefix"        # see Tasks Runner for more information
paranoid = false              # see MISE_PARANOID

shorthands_file = '~/.config/mise/shorthands.toml' # path to the shorthands file, see `MISE_SHORTHANDS_FILE`
disable_default_shorthands = false                 # disable the default shorthands, see `MISE_DISABLE_DEFAULT_SHORTHANDS`

env_file = '.env' # load env vars from a dotenv file, see `MISE_ENV_FILE`

experimental = true # enable experimental features

# configure messages displayed when entering directories with config files
status = { missing_tools = "if_other_versions_installed", show_env = false, show_tools = false }
idiomatic_version_file_enable_tools = ["python"]

# "_" is a special key for information you'd like to put into mise.toml that mise will never parse
[_]
foo = "bar"
