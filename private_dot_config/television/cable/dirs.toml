[metadata]
name = "dirs"
description = "A channel to select from directories"
requirements = ["fdfind"]

[source]
command = ["fdfind -t d", "fdfind -t d --hidden"]

[preview]
command = "ls -la --color=always '{}'"

[keybindings]
shortcut = "f2"
