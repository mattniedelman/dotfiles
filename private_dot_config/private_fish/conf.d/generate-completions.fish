# Fish completions are generated via two mechanisms:
# 1. mise post-install hook: runs fish_probe_completion after each `mise install`
# 2. Manual: run `fish-completions-refresh` to probe all current shims
#
# Nothing runs automatically at shell startup to avoid blocking.
