[project]
name = "augment-hooks"
version = "0.1.0"
description = "Unified hooks for Augment CLI and Claude Code"
requires-python = ">=3.11"
dependencies = [
    "cchooks>=0.1.0",
]

[dependency-groups]
dev = []

